# nginx.conf: ShunyaLink High-Availability Load Balancer ⚖️🚀

user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for" '
                      'TO: $upstream_addr'; # Track which backend node was hit!

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # ── Upstream: The distributed backend cluster 🏗️ ──
    upstream backend_cluster {
        server backend:8080;
    }

    server {
        listen       80;
        server_name  localhost;

        # ── Health Check Endpoint 🏥 ──
        location /health {
            proxy_pass http://backend_cluster/actuator/health;
        }

        # ── Main API & Redirection Proxy 🌐 ──
        location / {
            proxy_pass http://backend_cluster;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 90;
            proxy_send_timeout 90;
            proxy_read_timeout 90;
        }
    }
}
